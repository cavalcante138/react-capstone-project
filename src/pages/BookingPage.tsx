import { BookingForm } from "../components/BookingForm"

export const BookingPage = () => {
    return (
        <div className="booking-page">
            <h1>Booking Page</h1>

            <BookingForm />
        </div>
    )
}